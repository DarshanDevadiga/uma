# Override global store directory to reside locally within workspace node_modules
store-dir=node_modules/.pnpm-store

# Force copy method to prevent cross-device hard link errors
package-import-method=copy

# Disable side effects cache to prevent metadata corruption
side-effects-cache=false
